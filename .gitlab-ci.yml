stages:
  - format
  - lint
  - test
  - coverage
  - build
  - bench
  - doc

test:lint:rustfmt:
  stage: format
  image: "guangie88/rustfmt-clippy:stable"
  script:
    - cargo fmt --all -- --check
  tags:
    - linux
  allow_failure: true
  when: always
    
test:lint:clippy:
  stage: lint
  image: "guangie88/rustfmt-clippy:stable"
  script:
    - cargo clippy -- -D warnings
  tags:
    - linux
  allow_failure: true
  needs: ["test:lint:rustfmt"]

    
test:test:linux:
  stage: test
  image: "guangie88/rustfmt-clippy:stable"
  script:
    - rustc --version
    - cargo --version
    - cargo install cargo-junit
    - cargo junit --name linux.xml
  cache:
    key: "$CI_COMMIT_REF_SLUG-test"
    paths:
      - ~/.cargo
      - target
  tags:
    - linux
  needs: ["test:lint:rustfmt"]
  artifacts:
    paths:
      - linux.xml
    reports:
      junit: linux.xml

test:test:windows:
  stage: test
  script:
    - choco install rustup.install -y --ignore-checksums
    - refreshenv
    - tree
    - C:/Users/gitlab_runner/.cargo/bin/cargo --version
    - C:/Users/gitlab_runner/.cargo/bin/rustc --version
    - C:/Users/gitlab_runner/.cargo/bin/cargo test
  cache:
    key: "$CI_COMMIT_REF_SLUG-test"
    paths:
      - ~/.cargo
      - target
  tags:
    - windows
  needs: ["test:lint:rustfmt"]
  only:
    - master
    - tags
  artifacts:
    paths:
      - windows.xml
    reports:
      junit: windows.xml

test:coverage:
  stage: coverage
  image: "thatonelutenist/rust-nathan:latest"
  script:
    - cargo check
    - cd asuran-core
    - cargo kcov
    - cd ../asuran
    - cargo kcov
    - cd ..
    - bash <(curl -s https://codecov.io/bash) -s target/cov
  cache:
    key: "$CI_COMMIT_REF_SLUG-cov"
    paths:
      - ~/.cargo
      - target
  tags:
    - linux
  needs: ["test:lint:rustfmt"]

build:build:linux:
  stage: build
  image: "guangie88/rustfmt-clippy:stable"
  script:
    - cargo build --release
  artifacts:
    paths:
      - target/release/
  cache:
    key: "$CI_COMMIT_REF_SLUG-build"
    paths:
      - ~/.cargo
      - target
  tags:
    - linux
  needs: ["test:test:linux"]
  only:
    - master
    - tags

build:build:windows:
  stage: build
  script:
    - choco install rust-ms -y
    - cargo --version
    - rustc --version
    - cargo build --release
  artifacts:
    paths:
      - target/release/
  cache:
    key: "$CI_COMMIT_REF_SLUG-build"
    paths:
      - ~/.cargo
      - target
  tags:
    - windows
  needs: ["test:test:windows"]
  only:
    - master
    - tags

build:bench:linux:
  stage: bench
  image: "guangie88/rustfmt-clippy:stable"
  variables:
    RUSTFLAGS: "-C target-cpu=native"
  script:
    - cargo bench
  cache:
    key: "$CI_COMMIT_REF_SLUG-bench"
    paths:
      - ~/.cargo
      - target
  tags:
    - linux
  needs: ["test:lint:rustfmt"]
  only:
    - master
    - tags

build:bench:windows:
  stage: bench
  variables:
    RUSTFLAGS: "-C target-cpu=native"
  script:
    - choco install rust-ms -y
    - cargo --version
    - rustc --version
    - cargo bench
  cache:
    key: "$CI_COMMIT_REF_SLUG-bench"
    paths:
      - ~/.cargo
      - target
  tags:
    - windows
  needs: ["test:lint:rustfmt"]
  only:
    - master
    - tags

pages:
  stage: doc
  image: "guangie88/rustfmt-clippy:stable"
  script:
    - cargo doc --no-deps --document-private-items
    - mv target/doc public
    - echo '<meta http-equiv="refresh" content="0; url=asuran">' > public/index.html
  artifacts:
    paths:
      - public
      - target
  needs: ["test:lint:rustfmt"]
  tags:
    - linux
  when: always
